import { Stack, Typography } from '@mui/material'

export function NewReservationPage() {
  return (
    <Stack spacing={2}>
      <Typography variant="h4">Nova Reserva</Typography>
      <Typography color="text.secondary">Formulário de criação será implementado na próxima fase.</Typography>
    </Stack>
  )
}
